using UnityEngine;

public class PLTEnhance : MonoBehaviour
{
    public float curu = 1;
    public int amount = 0;

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
